/**
 * @deprecated Use `apiFetch` and `fetchApi` from `@/lib/api-client` instead.
 *
 * This file is kept only for backward compatibility during migration.
 * All new code should import directly from `@/lib/api-client`.
 */

export { apiFetch as authFetch, apiFetch as authApi } from "./api-client";
export default undefined;
