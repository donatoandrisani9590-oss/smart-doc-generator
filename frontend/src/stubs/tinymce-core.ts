/**
 * TinyMCE core runtime stub — provides Editor class shape.
 * Replaced by Tiptap in Phase 6.
 */
export class Editor {
  getContent() { return '' }
  setContent() {}
  on() {}
  off() {}
  destroy() {}
  getBody() { return document.createElement('div') }
}
export default Editor
