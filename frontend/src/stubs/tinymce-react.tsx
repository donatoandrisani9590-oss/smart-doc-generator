/**
 * @tinymce/tinymce-react runtime stub — placeholder Editor component.
 * Replaced by Tiptap in Phase 6.
 */
export function Editor(_props: Record<string, any>) {
  return (
    <div
      className="flex items-center justify-center border border-dashed border-warm-300 rounded-xl p-8 text-muted-foreground text-sm min-h-[200px]"
      data-testid="editor-placeholder"
    >
      Editor wird in Phase 6 durch Tiptap ersetzt
    </div>
  )
}
